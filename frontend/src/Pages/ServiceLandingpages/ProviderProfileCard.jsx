import UnifiedProfileCard from '../../components/shared/UnifiedProfileCard';

const ProviderProfileCard = ({ serviceData }) => {
  return <UnifiedProfileCard serviceData={serviceData} mode="edit" />;
};

export default ProviderProfileCard;
