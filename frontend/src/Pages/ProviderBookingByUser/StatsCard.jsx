import UnifiedStatsCard from '../../components/shared/UnifiedStatsCard';

const StatsCard = ({ providerId }) => {
  return <UnifiedStatsCard providerId={providerId} mode="default" />;
};

export default StatsCard;
